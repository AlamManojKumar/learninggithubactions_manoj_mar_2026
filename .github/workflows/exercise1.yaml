---
name: exercise-1

on:
    push:
        branches:
            - main
jobs:
   learning:
       name: learning how to write the job
       runs-on: ubuntu-latest
       steps:
           - name: learning
             run: "echo hello how are you"
           - name: welcome
             run: "echo Welcome to github actions"
           - name: listenvvariables
             run:  printenv